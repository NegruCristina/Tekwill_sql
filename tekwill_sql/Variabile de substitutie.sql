SELECT *
FROM employees
WHEre first_name = 'Jennifer';


SELECT *
FROM employees
WHEre first_name = '&first_name';



SELECT *
FROM EMPLOYEES
WHERE HIRE_DATE = '&DATE_VAR';





SELECT FIRST_NAME,
       LAST_NAME,
       &HIDE_COLUMN
FROM EMPLOYEES
WHERE HIRE_DATE = '&DATE_VAR'
ORDeR BY &HIDE_COLUMN;




SELEct EMPLOYEE_ID,
      LAST_NAME,
      JOB_ID,
      &&HIDE
fROM EMPLOYEES
ORDER BY &COLUMN_NAME DESC;




SELECT EMPLOYEE_ID,
      LAST_NAME,
      SALARY,
      JOB_ID,
      &&HIDE_COLUMN
FROM EMPLOYEES
ORDER BY &COLUMN_NAME DESC;




SELECT EMPLOYEE_ID,
      LAST_NAME,
      SALARY,
      JOB_ID,
      &&HIDE_COLUMN,
      &&DEPARTMENT_NAME
FROM EMPLOYEES
ORDER BY &COLUMN_NAME DESC,
          &department_id asc;

undefine hide_column;



Define hide_column ='17-JUN-11'
SELECT EMPLOYEE_ID,
      LAST_NAME,
      SALARY,
      JOB_ID,
FROM EMPLOYEES
WHERE HIRE_DATE = '&HIRE_COLUMN'
ORDER BY &COLUMN_NAME DESC;


UNDEFINE  HIRE_DATE,




SET VERIFY ON
SELECT EMPLOYEE_ID,
      LAST_NAME,
      SALARY,
      JOB_ID,
      &&HIDE_COLUMN,
      &&DEPARTMENT_NAME
FROM EMPLOYEES
ORDER BY &COLUMN_NAME DESC,
          &department_id asc;
          
          
 UNDEFINE HIDE_COLUMN,
 UNDEFINE DEPARTMENT_NAME
 UNDEFINE COLUMN_NAME
 UNDEFINE DEPARTMENT_ID
 
 
 
 
 SET VERIFY ON
 SELECT EMPLOYEE_ID,
        LAST_NAME, 
        SALARY
FROM EMPLOYEES
WHERE EMPLOYEE_ID = &EMPLOYEE_NUME;